<resources>

    <style name="PedidosTheme" parent="PedidosThemeBase"/>
    <!-- Base application theme. -->
    <!-- App Style -->
    <style name="PedidosThemeBase" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorControlNormal">@color/windowBackground</item>
    </style>

    <!-- Button Style. -->
    <style name="PedidosButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorPrimary</item>

    </style>

    <style name="PedidosButtonDark" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>

    </style>

    <!-- Button Cancelar Style. -->
    <style name="PedidosButtonCancelar" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorError</item>

    </style>


    <!--Campo de texto-->
    <style name="PedidosEditText" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorHint">@color/textColorSecondary</item>
        <item name="android:textColorPrimary">@color/textColorSecondary</item>
        <item name="colorAccent">@color/textColorSecondary</item>
        <item name="colorControlNormal">@color/textColorSecondary</item>
        <item name="colorControlActivated">@color/textColorSecondary</item>

    </style>

    <!--Campo de texto-->
    <style name="PedidosEditTextError" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/colorError</item>
    </style>

    <style name="PedidosDialogAlert" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/textColor</item>
        <item name="android:background">@color/windowBackground</item>
    </style>

    <style name="PedidosText" parent="Theme.AppCompat.Light">
        <item name="android:textColor">@color/colorAccent</item>

    </style>

</resources>
